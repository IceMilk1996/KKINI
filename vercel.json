{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/recipe/share/:slug", "destination": "/api/share?slug=:slug" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
